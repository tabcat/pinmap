pinmap could use blockstore only, but one is not going to be 

a global blockstore like normal
a bunch of datastores that keep (id -> pin/unpin and time)

wondering if pinmap should just be the latter

it should probably also be a dictionary


